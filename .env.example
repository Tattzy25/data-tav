# Copy this file to .env.local (or your deployment's environment settings)
# and provide the real values. No defaults are shipped with the app.

# Path (relative to the repo root) or absolute path to a JSON registry file
# that lists every model you want to expose. See config/ai-model-registry.example.json
# for the required shape.
AI_MODEL_REGISTRY_FILE=config/ai-model-registry.json

# Optional: Inline JSON instead of pointing to a file.
# AI_MODEL_REGISTRY=

# Provider specific API keys (can be overridden per-request via the Custom API popover)
OPENAI_API_KEY=
GROQ_API_KEY=
